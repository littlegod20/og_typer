# Leave empty in dev to use Vite proxy (/api, /auth → localhost:3000)
VITE_API_BASE=

# Production (e.g. Vercel): public API origin only, no /api suffix
# VITE_API_BASE=https://your-api.up.railway.app
